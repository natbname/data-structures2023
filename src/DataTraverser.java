public interface DataTraverser
{
    //no global variables here. no variables at all really
    int next(); //iterates through each list
    boolean hasNext(); //checks if there is a next value
}

    